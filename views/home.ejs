<%- include("partials/header.ejs") %>

<main>
    <% if(locals.blogs.length) { %>
        <ul>
            <% blogs.forEach(blog => { %>
                <li>
                    <h3><%= blog %></h3>
                    <form>
                        <button type="submit" name="blog-title" value="<%= blog %>" formaction="/view">View</button>
                        <button type="submit" name="blog-title" value="<%= blog %>" formaction="/edit">Edit</button>
                        <button type="submit" name="blog-title" value="<%= blog %>" formaction="/delete">Delete</button>
                    </form>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>Nothing here for now!</p>
    <% } %>
</main>

<%- include("partials/footer.ejs") %>
